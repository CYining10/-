#include <stdio.h>
int main()
{
    int m, n, i, j, flag, count, temp;
    int a[250], b[105];
    while (scanf("%d %d", &m, &n) != EOF && (m != 0 || n != 0))
    {
        count = 0;
        for (i = 0; i < m + n; i++)
            scanf("%d", &a[i]);
        for (i = 0; i < m; i++)
        {
            flag = 0;
            for (j = m; j < m + n; j++)
            {
                if (a[i] == a[j])
                {
                    flag = 1;
                    break;
                }
            }
            if (flag == 0)
            {
                b[count] = a[i];
                count++;
            }
        }
        for (i = 0; i < count; i++)
        {
            for (j = 0; j < count - i - 1; j++)
            {
                if (b[j] > b[j + 1])
                {
                    temp = b[j];
                    b[j] = b[j + 1];
                    b[j + 1] = temp;
                }
            }
        }
        if (count == 0)
            printf("NULL\n");
        else
        {
            for (i = 0; i < count; i++)
            {
                if (count - i > 1)
                    printf("%d ", b[i]);
                else
                    printf("%d", b[i]);
            }
            printf("\n");
        }
    }
    return 0;
}
